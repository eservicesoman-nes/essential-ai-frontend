# Dormant Files

Files in this folder are **not currently used anywhere** in the live application. They are kept here for reference only, not deleted, in case something in them is ever needed.

## How files get here
Before moving any file into this folder, confirm it is genuinely unused: search the full codebase for any reference to its filename or path (e.g., a GitHub code search across the whole repo). Only move a file here once that search comes back with zero real matches.

## Files currently here

- **index-legacy.html** — an old, unused version of the main app page. Confirmed via full-repo code search (zero references anywhere) on 2026-08-28.

## If you need to restore something from here
Before moving anything back out of this folder into active use, re-confirm it's actually needed and re-check for naming conflicts with current active files — the codebase may have changed since the file was moved here.
